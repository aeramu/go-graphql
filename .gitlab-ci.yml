image: golang:latest

variables:
  PATH: github.com/aeramu/go-graphql

before_script:
  - mkdir -p $GOPATH/src/$PATH
  - cp -r $CI_PROJECT_DIR $GOPATH/src/$PATH
  - ln -s $CI_PROJECT_DIR $GOPATH/src/$PATH
  - cd $GOPATH/src/$PATH
  - ls
  - go get -v -d ./...

stages:
 - test
 - build
 - deploy

unit_tests:
  stage: test
  script:
    - go build -v ./...